<div class="right-side" {{#is display 'set'}}style="display: none;"{{/is}}>
    <div class="content-header">
        <h3 class="content-title clearfix">
            Список сказок

            <div class="pull-right btn btn-primary addTale">
                <i class="fa fa-plus"></i>
                &nbsp;Добавить сказку
            </div>
        </h3>
    </div>
    <div class="content">
        {{#if tales.isResolved}}
            {{#if tales.length}}
                <table class='table'>
                    <thead>
                        <th>Название</th>
                        <th>Количество слайдов</th>
                        <th>Активирована</th>
                        <th>Действия</th>
                    </thead>
                    <tbody>
                        {{#sortedBy tales '_id' 'desc'}}
                            <tr class="tale{{#is type 0}} info{{/is}} {{#unless shared}} danger{{/unless}}" {{data "tales"}}>
                                <td>{{name}}</td>
                                <td>
                                    <span class="label label-primary">{{frames.length}}</span>
                                </td>
                                <td>
                                    {{#if active}}
                                        <span class="label label-success">Активированая</span>
                                    {{else}}
                                        <div class="btn btn-warning btn-sm activate">Активировать сказку</div>
                                    {{/if}}
                                </td>
                                <td>
                                    <div class="btn btn-sm btn-default editTale">
                                        <i class="fa fa-pencil"></i>
                                    </div>
                                    <div class="btn btn-sm btn-danger removeTale">
                                        <i class="fa fa-trash-o"></i>
                                    </div>
                                    <a href="/fairy-tale/{{_id}}" target="_blank" class="btn btn-sm btn-primary">
                                        <i class="fa fa-eye"></i>
                                    </a>
                                </td>
                            </tr>
                        {{/sortedBy}}
                    </tbody>
                </table>
            {{else}}
                <div class="alert alert-info">
                    <i class="fa fa-info"></i>
                    <b>Примечание! </b>
                    Ни одной сказки еще не добавлено!
                </div>
            {{/if}}
        {{/if}}
    </div>
</div>
<div class="taleForm" {{#is display 'list'}}style="display: none;"{{/is}}>

</div>
